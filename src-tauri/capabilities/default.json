{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for dev",
  "windows": ["capture", "history-report"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:webview:allow-print",
    "dialog:default"
  ]
}
